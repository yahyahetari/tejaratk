# 🛍️ تجارتك - منصة SaaS متكاملة

منصة SaaS متكاملة لإنشاء وإدارة المتاجر الإلكترونية مع نظام إدارة كامل للتجار والمسؤولين.

## ✨ الميزات الرئيسية

### 🔐 نظام المصادقة والأمان
- ✅ تسجيل دخول آمن بـ JWT
- ✅ نظام رمز التحقق للمسؤولين (2FA)
- ✅ حماية الـ API Routes
- ✅ المسؤول الرئيسي: `yahyahetari77@gmail.com`

### 🏪 إدارة المتجر (للتجار)
- ✅ إدارة المنتجات (إضافة، تعديل، حذف)
- ✅ دعم متغيرات المنتجات (الحجم، اللون، إلخ)
- ✅ رفع صور متعددة للمنتجات
- ✅ إدارة الفئات والفئات الفرعية
- ✅ فلاتر متقدمة:
  - المنتجات الأكثر مبيعاً
  - المنتجات التي نفذ مخزونها
  - المنتجات ذات المخزون المنخفض
- ✅ إدارة الطلبات مع تتبع الحالة
- ✅ إدارة العملاء
- ✅ تقارير شاملة مع:
  - تصدير PDF
  - مخططات بيانية (أسبوعية، شهرية، سنوية)
  - تحليل المبيعات والأرباح
  - أفضل المنتجات مبيعاً

### 👨‍💼 لوحة تحكم المسؤول
- ✅ إدارة جميع التجار
- ✅ عرض إحصائيات شاملة للمنصة
- ✅ تحليلات متقدمة
- ✅ مراقبة الإيرادات والأرباح
- ✅ تقارير مفصلة

## 🗄️ قاعدة البيانات

**PostgreSQL + Prisma ORM**

### النماذج الرئيسية:
- `User` - المستخدمين (تجار ومسؤولين)
- `VerificationCode` - رموز التحقق
- `Merchant` - التجار
- `Product` - المنتجات
- `ProductVariant` - متغيرات المنتجات
- `ProductImage` - صور المنتجات
- `Category` - الفئات
- `Order` - الطلبات
- `OrderItem` - عناصر الطلبات
- `Customer` - العملاء

## 🚀 التثبيت والإعداد

### المتطلبات
- Node.js >= 18.0.0
- PostgreSQL >= 13
- npm أو yarn أو pnpm

### خطوات التثبيت

1. **تثبيت المكتبات:**
```bash
npm install
```

2. **إعداد قاعدة البيانات:**

أنشئ ملف `.env` في المجلد الرئيسي:
```env
DATABASE_URL="postgresql://user:password@localhost:5432/tejaratk?schema=public"
JWT_SECRET="your-super-secret-jwt-key"
ADMIN_EMAIL="yahyahetari77@gmail.com"
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT="587"
EMAIL_USER="your-email@gmail.com"
EMAIL_PASS="your-app-password"
```

3. **تشغيل Prisma:**
```bash
# توليد Prisma Client
npx prisma generate

# إنشاء قاعدة البيانات
npx prisma db push

# (اختياري) فتح Prisma Studio
npx prisma studio
```

4. **تشغيل المشروع:**
```bash
# وضع التطوير
npm run dev

# بناء للإنتاج
npm run build

# تشغيل الإنتاج
npm start
```

المشروع سيعمل على: `http://localhost:3000`

## 📁 هيكل المشروع

```
tejaratk-merged/
├── app/
│   ├── api/                    # API Routes
│   │   ├── auth/              # المصادقة
│   │   │   ├── login/         # تسجيل الدخول
│   │   │   ├── register/      # التسجيل
│   │   │   ├── verify-code/   # التحقق من الرمز
│   │   │   └── logout/        # تسجيل الخروج
│   │   ├── store/             # API المتجر
│   │   │   ├── products/      # المنتجات
│   │   │   ├── orders/        # الطلبات
│   │   │   ├── customers/     # العملاء
│   │   │   ├── categories/    # الفئات
│   │   │   └── reports/       # التقارير
│   │   └── admin/             # API المسؤول
│   │       ├── merchants/     # التجار
│   │       ├── analytics/     # التحليلات
│   │       ├── products/      # جميع المنتجات
│   │       └── orders/        # جميع الطلبات
│   ├── (auth)/                # صفحات المصادقة
│   ├── (dashboard)/           # لوحة تحكم التاجر
│   ├── (admin)/               # لوحة تحكم المسؤول
│   └── globals.css            # التصميم العام
├── components/                # React Components
│   ├── ui/                    # مكونات UI
│   ├── layout/                # مكونات التخطيط
│   ├── auth/                  # مكونات المصادقة
│   ├── dashboard/             # مكونات لوحة التحكم
│   ├── store/                 # مكونات المتجر
│   └── admin/                 # مكونات المسؤول
├── lib/                       # دوال مساعدة
│   ├── auth/                  # دوال المصادقة
│   │   ├── jwt.js            # JWT
│   │   ├── password.js       # كلمات المرور
│   │   ├── session.js        # الجلسات
│   │   └── verification-code.js  # رموز التحقق
│   ├── db/                    # قاعدة البيانات
│   │   └── prisma.js         # Prisma Client
│   ├── email/                 # البريد الإلكتروني
│   │   └── sender.js         # إرسال البريد
│   ├── store/                 # دوال المتجر
│   └── utils/                 # دوال عامة
│       └── pdf-generator.js  # توليد PDF
├── prisma/
│   └── schema.prisma          # Schema قاعدة البيانات
├── config/                    # ملفات الإعدادات
├── middleware/                # Middleware
├── public/                    # ملفات عامة
├── .env.example               # مثال ملف البيئة
├── package.json
├── next.config.js
├── tailwind.config.js
└── README.md
```

## 🔑 الحسابات الافتراضية

### المسؤول الرئيسي
- البريد الإلكتروني: `yahyahetari77@gmail.com`
- يتم إرسال رمز التحقق عند تسجيل الدخول
- صلاحيات كاملة على المنصة

### إنشاء حساب تاجر
1. انتقل إلى صفحة التسجيل
2. أدخل البيانات المطلوبة
3. سيتم إنشاء الحساب مباشرة

## 📊 API Endpoints

### المصادقة
- `POST /api/auth/register` - التسجيل
- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/auth/verify-code` - التحقق من الرمز
- `POST /api/auth/logout` - تسجيل الخروج

### المتجر (للتجار)
- `GET /api/store/products` - جلب المنتجات (مع الفلاتر)
- `POST /api/store/products` - إضافة منتج
- `GET /api/store/orders` - جلب الطلبات
- `POST /api/store/orders` - إنشاء طلب
- `GET /api/store/reports` - جلب التقارير

### المسؤول
- `GET /api/admin/merchants` - جلب جميع التجار
- `GET /api/admin/analytics` - جلب التحليلات
- `GET /api/admin/products` - جلب جميع المنتجات
- `GET /api/admin/orders` - جلب جميع الطلبات

## 🎨 التصميم

- **Framework**: Tailwind CSS
- **Components**: Radix UI
- **Icons**: Lucide React
- **Charts**: Recharts
- **Font**: Cairo (عربي)
- **Direction**: RTL

## 📈 الميزات المتقدمة

### فلاتر المنتجات
```javascript
// الأكثر مبيعاً
GET /api/store/products?filter=best-selling

// نفذ المخزون
GET /api/store/products?filter=out-of-stock

// مخزون منخفض
GET /api/store/products?filter=low-stock
```

### التقارير
```javascript
// تقرير أسبوعي
GET /api/store/reports?period=week

// تقرير شهري
GET /api/store/reports?period=month

// تقرير سنوي
GET /api/store/reports?period=year
```

### تصدير PDF
```javascript
import { generateReportPDF } from '@/lib/utils/pdf-generator';

const doc = generateReportPDF(reportData, merchantName);
doc.save('report.pdf');
```

## 🔒 الأمان

- ✅ JWT للمصادقة
- ✅ HttpOnly Cookies
- ✅ تشفير كلمات المرور (bcrypt)
- ✅ رموز التحقق للمسؤولين
- ✅ حماية CSRF
- ✅ Headers أمنية
- ✅ Rate Limiting (يمكن إضافته)

## 📝 ملاحظات مهمة

1. **المسؤول الرئيسي**: يتم تحديده تلقائياً من خلال `ADMIN_EMAIL` في `.env`
2. **رمز التحقق**: يُرسل فقط للمسؤولين عند تسجيل الدخول
3. **التجار**: يسجلون الدخول مباشرة بدون رمز تحقق
4. **قاعدة البيانات**: Prisma + PostgreSQL فقط (لا MongoDB)
5. **التصميم**: موحد باستخدام Tailwind CSS و Radix UI

## 🛠️ التطوير

### إضافة ميزة جديدة
1. أنشئ API Route في `app/api/`
2. أنشئ Component في `components/`
3. أنشئ صفحة في `app/`
4. حدّث Prisma Schema إذا لزم الأمر

### اختبار المشروع
```bash
# تشغيل وضع التطوير
npm run dev

# فتح Prisma Studio
npx prisma studio

# فحص الأكواد
npm run lint
```

## 📞 الدعم

للدعم والاستفسارات:
- البريد الإلكتروني: yahyahetari77@gmail.com

## 📄 الترخيص

جميع الحقوق محفوظة © 2024 تجارتك

---

**تم الدمج بنجاح! ✨**

المشروع جاهز للاستخدام مع جميع الميزات المطلوبة:
- ✅ دمج كامل للأكواد
- ✅ تصميم موحد
- ✅ فلاتر المنتجات
- ✅ تقارير PDF
- ✅ مخططات بيانية
- ✅ نظام المسؤول مع رمز التحقق
- ✅ Prisma + PostgreSQL فقط
